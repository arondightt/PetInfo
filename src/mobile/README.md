# src/mobile

App mobile do PetTag — React Native + Expo (fase futura).

Consome a mesma API PHP em `src/api/`.

**Deploy:** Expo EAS Build (automático via GitHub Actions ao alterar `src/mobile/`)

Consulte [../../documentation/architecture.md](../../documentation/architecture.md) para referência.
